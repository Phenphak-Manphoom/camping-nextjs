import { Input } from "../ui/input";

const Search = () => {
  return (
    <Input
      type="text"
      placeholder="Search for a campsite"
      className="max-w-xs"
    />
  );
};
export default Search;
